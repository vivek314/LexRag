# Copy this to .env and fill in values. NEVER commit .env!

# OpenAI (optional) — app works without this using open-source LLM
OPENAI_API_KEY=

# HuggingFace token (optional) — improves rate limits on free HF Inference API
HF_TOKEN=
